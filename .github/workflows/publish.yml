name: Publish Packages

on:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - uses: pnpm/action-setup@v3
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://npm.pkg.github.com"
          scope: "@coolcinema"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        env:
          COOLCINEMA_GH_PKG_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # --- NEW: Auto-Vendoring Step ---
      - name: Auto-Vendor External Protos
        run: |
          echo "🔄 Running vendor scripts..."
          # Запускаем скрипт вендоринга только для contracts (если он там есть)
          if pnpm --filter @coolcinema/contracts run vendor --if-present; then
            echo "Vendor script executed."
          else
            echo "No vendor script found or failed."
          fi

          # Проверяем, изменилось ли что-то
          if [ -n "$(git status --porcelain)" ]; then
            echo "📦 Vendor script generated changes. Committing..."
            git add packages/contracts/protos
            git commit -m "chore(vendor): update external protos [skip ci]"
            git push
            echo "✅ Push successful."
          else
            echo "✅ No vendor changes detected."
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # --- Version Bumping Step ---
      - name: Bump Versions (Dynamic Smart Patch)
        id: bump
        run: |
          echo "🔍 Analyzing changes..."

          CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD)

          echo "📝 Raw Git Diff Output:"
          echo "$CHANGED_FILES"
          echo "-----------------------------------"

          FILTERS=""
          CHANGED=false

          # Ищем строки, содержащие 'packages/'
          # Добавляем || true, чтобы grep не валил скрипт
          PATHS_WITH_PACKAGES=$(echo "$CHANGED_FILES" | grep "packages/" || true)

          if [ -z "$PATHS_WITH_PACKAGES" ]; then
            echo "🚫 No changes found in packages/ directory."
          else
             # Извлекаем уникальные директории пакетов
             CHANGED_DIRS=$(echo "$PATHS_WITH_PACKAGES" | sed -n 's/.*\(packages\/[^/]*\).*/\1/p' | sort | uniq)

             for dir in $CHANGED_DIRS; do
                if [ -d "$dir" ] && [ -f "$dir/package.json" ]; then
                   PKG_NAME=$(node -p "require('./$dir/package.json').name")
                   OLD_VERSION=$(node -p "require('./$dir/package.json').version")
                   
                   echo "📦 Detected change in: $PKG_NAME ($dir) [Current: $OLD_VERSION]"
                   
                   FILTERS="$FILTERS --filter $PKG_NAME"
                   CHANGED=true
                   
                   # Сразу бампаем версию
                   echo "   Running npm version patch..."
                   cd $dir
                   npm version patch --no-git-tag-version
                   NEW_VERSION=$(node -p "require('./package.json').version")
                   echo "   ✅ New version: $NEW_VERSION"
                   cd ../..
                fi
             done
          fi

          if [ "$CHANGED" = "true" ]; then
             echo "🚀 Ready to build and publish."
             echo "changed=true" >> $GITHUB_OUTPUT
             echo "filter=$FILTERS" >> $GITHUB_OUTPUT
          else
             echo "💤 No packages changed."
             echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Clean Artifacts
        if: steps.bump.outputs.changed == 'true'
        run: rm -rf packages/*/dist packages/*/*.tsbuildinfo

      - name: Build Changed Packages
        if: steps.bump.outputs.changed == 'true'
        run: pnpm ${{ steps.bump.outputs.filter }} run build

      - name: Publish Changed Packages
        if: steps.bump.outputs.changed == 'true'
        run: pnpm ${{ steps.bump.outputs.filter }} publish --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COOLCINEMA_GH_PKG_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit Version Bumps
        if: steps.bump.outputs.changed == 'true'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(release): update versions [skip ci]"
          file_pattern: "packages/**/package.json"
